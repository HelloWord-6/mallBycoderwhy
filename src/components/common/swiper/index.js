// 导入文件
import Swiper from './Swiper'
import SwiperItem from './SwiperItem'
//用对象统一导出 如果想在home用上面两个组件 要先导入两个组件
//如果下面这样统一导出的话，就直接写
//import {Swiper, SwiperItem} from ‘ ’  就可以了   因为这里导出是一个对象
export {
  Swiper, SwiperItem
}
